version: '3.8'
services:
  tsto-server:
    build:
      context: .
      dockerfile: Dockerfile.multistage
      args:
        - CXXFLAGS=-O3 -Wno-error
    ports:
      - "8080:80"
    volumes:
      - ./data:/app/data
      - ./dlc:/app/dlc